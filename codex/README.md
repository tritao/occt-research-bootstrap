# Codex workflow

1) `source ./env.sh`
2) run `codex` once to authenticate
3) `just codex-mcp` to register MCP servers:
   - backlog (Backlog.md)
   - occt-lsp (clangd via mcp-language-server)

Then use prompts in `prompts/`.
