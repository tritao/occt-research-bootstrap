# Repro: <slug>

## Goal

One sentence describing what this repro proves and why it exists.

## Preconditions

- OCCT build: `build-occt/` exists and is up to date
- Provenance: `notes/maps/provenance.md` matches the build used

## How to run (OCCT oracle)

Command(s):
- `<command>`

## Outputs

- Output files:
  - `golden/<name>.json`
- Match criteria:
  - exact: <fields>
  - tolerant: <fields> within <epsilon>

## Scenarios covered / not covered

- Covered: …
- Not covered: …

