{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync": "../tools/py.sh ../tools/sync_starlight_site.py --root .. --site site --dest-subdir occt",
    "sync:clean": "../tools/py.sh ../tools/sync_starlight_site.py --root .. --site site --dest-subdir occt --clean",
    "predev": "npm run sync",
    "dev:watch": "node scripts/dev-watch.mjs",
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "npm run sync:clean",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.37.3",
    "astro": "^5.6.1",
    "astro-mermaid": "^1.3.1",
    "mermaid": "^11.12.2",
    "sharp": "^0.34.2",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "chokidar": "^4.0.3"
  }
}
